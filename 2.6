#6 Функция, чтобы проверить, находит ли число в заданном диапазоне
a = int(input("Число для проверки "))
b, c = list(map(int,input("Диапазон (без запятых) ").split()))


def proverka(a,b,c):
    if b <= a <= c:
        print("Входит")
    else:
        print("Не входит")
proverka(a,b,c)
